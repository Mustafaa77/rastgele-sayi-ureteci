GİRDİ: Seed_Değeri
DURUM = Hash(Seed_Değeri)

DÖNGÜ (İstenen Bit Sayısına Ulaşana Kadar):
   SAYAÇ += 1
   EĞER DURUM çift İSE:
      DURUM = DURUM / 2
   DEĞİLSE:
      DURUM = (3 * DURUM) + (DURUM XOR 0xABCDEF)
   
   EĞER DURUM <= 1 İSE:
      DURUM = Hash(SAYAÇ)

   B1 = DURUM'un 3. biti, B2 = DURUM'un 7. biti
   EĞER B1 != B2 İSE:
      BİT_EKLE(B1)
