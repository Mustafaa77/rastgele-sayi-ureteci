BAŞLA
  GİRDİ: Anahtar_Metni (Seed)
  DURUM = Hash(Anahtar_Metni) // Başlangıç entropisi
  
  FONKSİYON NextBit():
    SAYAC = SAYAC + 1
    EĞER (DURUM çift) İSE:
       DURUM = DURUM / 2
    DEĞİLSE:
       MASKE = (DURUM XOR 0xFF)
       DURUM = (3 * DURUM) + MASKE
    
    // Von Neumann Whitening
    BIT1 = DURUM'un 3. biti
    BIT2 = DURUM'un 7. biti
    EĞER (BIT1 != BIT2) İSE:
       DÖNDÜR BIT1
    DEĞİLSE:
       Tekrar dene (Recursive call)
  FONKSİYON BİTİR
BİTİR
